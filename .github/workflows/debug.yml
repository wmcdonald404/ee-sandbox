name: Debug workflow for experimentation

on: 
    workflow_dispatch

permissions:
  id-token: write
  contents: read

env:
  TOGGLE_STUFF: yes

jobs:
  deploy-stuff:
    name: deploy-stuff
    if: vars.DEPLOY_STUFF == 'yeah'
    runs-on: ubuntu-latest
    environment: test
    steps:        
      - name: Run a simple step 1
        run: echo "vars.DEPLOY_STUFF:" ${{ vars.DEPLOY_STUFF }}

  run-steps-if-true:
    runs-on: ubuntu-latest
    environment: test
    steps:  
      - name: Run a simple step 3
        if: vars.DEPLOY_STUFF == 'yeah'
        run: echo "vars.DEPLOY_STUFF:" ${{ vars.DEPLOY_STUFF }}

